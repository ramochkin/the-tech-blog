<div class="footer flex-row justify-center">
    <a href="/dashboard/new">➕ New Post</a>
</div>

<div class="posts">
  {{#each posts}}
  <article class="card">
    <div class="card-header flex-row justify-space-between">
      <h2>
        <a href="/dashboard/edit/{{id}}">{{post_title}}</a>
      </h2>
      <div>
        
        Posted on {{format_date createdAt}}
        
      </div>
    </div>

  </article>
  {{/each}}
</div>